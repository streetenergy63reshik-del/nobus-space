# Исправления после ручной проверки 8 сентября 2026 года

**REWORK / NOT ACCEPTED / NOT PUBLISHED.** C5–C6 HOLD; MVP1 NOT READY.

Владелец проверил временный e9bf22aee1187c06b6846a6debb865014c205f86
(tree 35684f40e5f99ed83d436fe1068bcdf891f96bf2) в настоящем Telegram.
Бот выполнил текстовую задачу; владелец получил и открыл TXT с тем же ответом.
Полного PASS нет: около 20 секунд не было подтверждения получения запроса;
Mini App показывал ошибку загрузки. [Исходные доказательства](LIVE-ATTEMPT3.json)
и два обезличенных снимка сохранены. История чата с посторонними запросами
не включена в пакет.

Причина задержки: первый ответ находился после await semantic admission.
Исправление отправляет честное подтверждение получения перед разбором, без
утверждения, что Core уже создал задачу. Admission и idempotency не меняются.

Mini App: шесть настоящих POST /api/session/recover вернули400; первичного
POST /api/session не было. Контроль без credentials: loopback Content-Length:0
вернул401, loopback chunked и оба HTTPS-варианта вернули400. Пустое тело
отклонялось по Transfer-Encoding до проверки cookie. Исправление допускает
единственный chunked без Content-Length, затем проверяет декодированное тело
с прежним нулевым лимитом и timeout. Непустое тело, неоднозначное framing,
чужой Origin, отсутствующая/прежняя cookie и старый bearer остаются запрещены.
Соответствие исправления фактическому HTTPS ещё требует повторного smoke.

Окно attempt3 остановлено до правок: graceful cleanup всех компонентов,
Windows Job0, wrappers exited, listener8765 отсутствует; HTTPS502, прежние
меню/webhook. Общий ledger сохранён:26 model reservations,20 actual turns,
358.124s; ASR2/14.11s, active0. Новое окно получит отдельные SHA/tree/state.

Предыдущие manifests, L1/L2/L3 и actual local runs относятся к своим исходным
ревизиям. Для нового цельного кандидата нужны собственные L1/L2/L3 и настоящий
owner-run. Опубликованная main остаётся принятой C3. Публикация C4 только после
полного PASS; permanent deploy/tag/release/C5/C6 не выполняются.

## Повторная проверка и запрос владельца на кнопки

На кандидате `90c867ca524df8b76ff3f2a9db4b50fb1ee2aff6`
(tree `80233ff1d0706e9b844212b085834a40ae94d1d2`) владелец подтвердил:
текстовая задача, быстрое сообщение о получении, итоговый ответ и TXT работают;
настоящий Mini App показывает ту же задачу, события и результат. Журнал HTTPS
подтверждает успешные создание сессии и восстановление после истечения bearer.
Нажатие «Копировать» и фактическое скачивание файла в Mini App пока не доказаны.

Голосовое окно attempt4 остановилось из-за слишком строгого тестового helper:
ASR вернул цифру «3» вместо слова «три», и helper отклонил синтетическую фразу.
Core не создал голосовую задачу. Единственный диагностический ASR подтвердил
причину; его расход включён в общий ledger. Helper получил ровно этот вариант,
без изменения распознанного текста. Ошибка 502 появилась после остановки
временного runtime; постоянного развёртывания в C4 нет.

В attempt5 расшифровка была получена. Отдельное сообщение «Да» не содержало
привязки к исходной записи, поэтому задачу не создало; пустой список Mini App
соответствовал Core. Владелец запросил подтверждение кнопкой и удаление
промежуточных сообщений. Окно остановлено до изменений: graceful shutdown,
Windows Job 0, wrappers завершены. Readback подтвердил прежнее меню, отсутствие
webhook и возврат HTTPS к исходному 502.

Новый кандидат реализует две кнопки под расшифровкой: «Подтверждаю» и
«Записать заново». Одноразовые действия используют существующее хранилище,
проверяют пользователя, чат, точное сообщение и расшифровку. Кнопки действуют
до 15 минут; повторное нажатие не создаёт вторую задачу. Замена отменяет старую
расшифровку, после чего владелец отправляет новую запись. Автоматический запуск
без подтверждения в C4 не включается. Исправленный текст ответом на исходную
запись остаётся допустимым.

Получение, распознавание, подтверждение и выполнение обновляют одно сообщение
бота. Оно удаляется после подтверждённой доставки результата и всех частей,
включая TXT. Неизвестный исход доставки сохраняет видимый статус; последующая
сверка и перезапуск не требуют повторного выполнения задачи. Пользовательские
сообщения и исторические сообщения прежних тестовых окон не удаляются.

Это новый непринимаемый по прежним доказательствам кандидат. Проверки и
настоящий owner-run привязываются к новой точной ревизии. C4 остаётся
REWORK / NOT ACCEPTED / NOT PUBLISHED; C5–C6 не начаты.


Независимая проверка нового UX выявила сбой удаления после «Записать заново»:
если Telegram не подтверждал удаление, завершённая отмена оставляла preview.
Исправление сверяет существующую запись завершённого голосового ввода и отсутствие
любой связанной работы, затем повторно читает Core под блокировкой очереди.
Сетевое удаление происходит после снятия блокировки. Общий лимит трёх попыток,
ASR-бюджет и сроки хранения голоса не изменены. Автоматическая сверка такой
отмены возможна пока сохранена существующая запись завершённого ввода (24 часа);
после её удаления неподтверждённое сообщение остаётся, чтобы не удалять его
по предположению. Исходные отклонённые доказательства сохранены.

## 8 сентября: проверка выполнения в настоящем локальном браузере

Owner smoke attempt6 на b89b651 прошёл: кнопка подтверждения голоса, итог, копирование и TXT523байта сверены в Telegram/Mini App. Временное окно остановилось по deadline, Job0 и rootreadback выполнены. Затем отдельная browser/HTTP/Core проверка с actual provider admission выявила дефект проекции: Core уже сохраняет STARTED/PARSING, а Mini App показывает «В очереди / ждёт выполнения». Поэтому b89b651 не принят целиком; его успешные scoped receipts остаются историческими.

Исправление меняет только PARSING→WORKING в общем каталоге продуктовых состояний. Core transitions, очередь и правило C3 о fail_recovery после прерванного PARSING сохранены. Два новых regression cases сначала упали на queued вместо working; после исправления прошли через настоящий Core/worker/HTTP с контролируемым отказом только на внешней provider boundary.

Одновременно исправлен только C4 test accounting: основной документ разрешает64 фактических model turns и5400с, включая startup/timeout/cleanup. Старый импорт C3 Budget ограничивал64 reservations, включая операции без model turn. Все исходные71строки (61model reservations,48marked turns,10ASR) сохранены; время и FAIL не списываются. C4 теперь атомарно резервирует потенциальный turn, маркирует его перед SDK и блокирует65-й turn. Startup не может быть превращён в model inference. C3 ledger/код не изменены; ASR20/1200с, единственный активный reservation и блокировка при неподтверждённом cleanup сохранены. Это соответствует исходному разрешению и не расширяет лимит64turns.

Новый freeze требует зависимых проверок, полного L1, независимых L2/L3 и настоящего bounded owner smoke своей ревизии. Неподвижные C1–C3/voice/model доказательства принимаются только с прежними SHA и отдельной проверкой применимости; их не переименовывают. Источники: .runtime/c4/visual-b89b651/GATE-FINDING.json, .runtime/c4/reviews/final-gap-b89b651/PROJECTION-RETEST-AND-BUDGET.md, running-red.xml и running-accounting-green.xml.

## Старый selected marker и незавершённый loader: owner attempt7

На66cff24 реальный Mini App восстановил selected marker задачи прежнего isolated
окна. До нового POST/tasks зарегистрированы GET task404; модальное окно сохраняло
«Загружаем…», а его «Обновить» снова читало тот же отсутствующий объект.
Новая задача после возврата к списку успешно прошла «В работе»→результат;
Core подтвердил Telegram answer/document356байт. Владелец подтвердил copy/download,
но HTTP artifact GET и физический скачанный файл в этом окне не наблюдались:
этот последний шаг не объявляется доказанным. Полный66cff24 Gate отклонён
по начальной ошибке UI; успешные scoped proofs сохраняются с исходным SHA.

Независимые L2 RED2 воспроизвели404 и initial503. Теперь оба места ошибки дают
одно и то же действие:404 возвращает к списку и закрывает только selected marker
через существующий dialog close handler; pending request не меняется. Ошибка
начального чтения убирает placeholder загрузки, а уже полученные данные при
временном сбое сохраняются. При новом выборе очищается время прежней карточки.
401 предлагает закрыть Mini App также внутри диалога. Это проекция и навигация,
без изменения Core/session/idempotency/queue. Статические URLs содержат digest
Git-содержимого, чтобы новый HTML не загружал прежний cached script тестового окна.

Attempt7 остановлен по запросу root после owner readback: Job0, wrappers closed,
HTTPS вернулся к502, bot/menu/webhook прежние. Изменённые bytes получают новый
freeze и зависимые проверки; прежние проверки не переименовываются.
