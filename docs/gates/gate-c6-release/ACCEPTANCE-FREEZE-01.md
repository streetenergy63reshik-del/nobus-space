# Зафиксированная приёмка C6

Кандидат `8f659d66f9d41b7dc2928b89f6d07423f4302f19`, tree `405c1f10e86d8a4e7faf7684c2d3432c5a53be2d`. Digest кода: `sha256:9bedab26845d319ddb7d8ea7c00af1f65f8d62f57a6fc669d902d71c7567df35`. Схема прежняя: `sha256:e5e371d440c752ca70c258b859528696eefe953023c26c1e2fd8d913dac2feeb`. Полные привязки — [ACCEPTANCE-FREEZE-01.json](ACCEPTANCE-FREEZE-01.json).

Критерии ниже — проекция ранее принятого контракта C6 и плана активации. Новых требований нет.

- **C6-01**. Сохранить перенесённые данные, прежние задачи, доставки и историю; canonical WIP не менять.
- **C6-02**. Один рабочий Core/poller/Job, semantic ON, квалифицированные worker/ASR и точная локальная и публичная readiness штатного supervisor.
- **C6-03**. Полное восстановление за ≤30мин: STOP, authenticated restore/reconciliation, native startup, readiness, private result/TXT и безопасная остановка с allowlist сверкой.
- **C6-04**. Управляемая копия моложе24ч;7daily/4weekly; реальный цикл Планировщика, verified decrypt/integrity, LastTaskResult0, restart/readiness и следующий запуск03:30.
- **C6-05**. Ограниченный штатный stop/start с сохранением accepted task/delivery watermark и без повторного исполнения или доставки.
- **C6-06**. Фактические owner text/voice/Mini App/download сценарии и ≥15мин наблюдения; synthetic evidence отдельно.
- **C6-07**. Явная итоговая приёмка владельца; затем ранее принятая постоянная Scheduler фаза с проверкой PID/Job/readiness и обязательной последующей backup.
- **C6-08**. Обновить ту же Word-памятку с visual QA; после приёмки опубликовать v1.0.2/release и итоговый статус, оставить принятый runtime работающим.

Подтверждённый блокер исправлен одним WIP-пакетом: одна строка HTTP-клиента и две проверки реального обмена. После заморозки выполнен единственный заключительный цикл: L1 —66 PASS, L2 —18 PASS, L3 —PASS; блокеров в исходниках нет. При неизменном digest эти проверки не повторяются. Проверки ещё не выполненных эксплуатационных критериев остаются частью исходной приёмки, а не новым циклом ревью.

Вне исходного контракта блокеров не добавлено; рекомендации не расширяют приёмку и записываются только в [FOLLOW-UP.md](FOLLOW-UP.md). Исходники кандидата заморожены. Новая выявленная проблема не даёт основания незаметно открыть ещё один цикл L2/L3.

Текущий эксплуатационный итог: предыдущий рабочий запуск безопасно остановлен из-за HTTP403 штатной readiness-команды. Все бизнес-данные сохранены, lease освобождён, offset неизменен; три задания Disabled.98,797с подтверждают функциональное восстановление и result/TXT, но полного RTO для исправленного supervisor ещё нет. Production/backup/restart/owner journeys/приёмка/релиз пока не подтверждены.
