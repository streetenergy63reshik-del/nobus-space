# Передача C6 — завершение MVP1

**9 сентября 2026: исправление промптов опубликовано; runtime остановлен; итоговая приёмка ожидается.** Защищённая main содержит PR №28 (`56a1092a`), исходник `2e64739` прошёл 631 проверку и разрешённую точечную независимую проверку. LIVE остаётся на `e3fbaf9`; supervisor завершился в 14:29:40 UTC с `runtime_failed`. Причина не установлена. [Восемь критериев](OPERATIONAL-STATUS-10.md).

Данные сохранены: четыре БД проходят integrity, прежние количества строк и 82 задачи не изменились. Зависший непринятый запрос безопасно закрыт; принятой заявке сохранена её задача. Новый код сохраняет условие владельца в следующем ответе на уточнение; материал промпта не выдаёт полномочий на упомянутые действия. [Проверка исправления](PROMPT-RECHECK-10.md).

Прежнее полное восстановление заняло 274,891 с; реальный цикл резервирования — 126,86 с с проверкой четырёх расшифрованных БД и штатным перезапуском. Эти факты относятся к e3fbaf9. Наблюдение 903,859 с прошло на прежнем процессе после отдельного неудачного интервала; причина первого сбоя не установлена. Текущую остановку эти результаты не отменяют.

## Оставшиеся пользовательские сценарии

Подтверждены три настоящих результата с TXT. После включения исправления остаются:

1. Текстовая просьба подготовить промпт с материалом, содержащим действия, и корректным уточнением при условии в ответе.
2. Голосовая просьба подготовить промпт: прочитать распознавание, подтвердить, получить результат и TXT.
3. Вторая последовательная задача Mini App: дождаться результата, повторно открыть и скачать связанный TXT без подмены первой задачей.

Это реальные действия через интерфейс владельца. Внутренний вызов Core или результат модели не доказывает просмотр распознавания либо скачивание файла. Доступные средства управления интерфейсом сейчас не запускаются из-за проверки доверенного RPC-пакета. Последняя просьба владельца отложить личное тестирование сохраняется; дальнейшее действие требует его участия или явного изменения согласованных критериев.

## Следующий исполнитель

1. Использовать новую опубликованную revision документации для C6-ACT-09; включить новый хэш контекста продукта, остановленный LIVE, точные четыре БД, сохранённую историю квоты и действия восстановления. Не повторять отклонённый C6-ACT-08.
2. Получить точное подтверждение пакета и общей дополнительной квоты: автоматическая проверка отклонила остановку/копирование БД/переключение на новый SHA до выполнения несмотря на общее разрешение владельца.
3. После включения проверить изменённый маршрут промпта, штатную готовность, новую копию, сверку accepted-task/delivery и наблюдение. Не повторять целиком неизменные исходные проверки без нового основания.
4. Завершить реальные сценарии и получить явную итоговую приёмку точной версии. Затем включить ранее согласованный постоянный профиль Планировщика и ежедневную копию в 03:30, выполнить обязательный цикл резервирования и проверить следующий запуск.
5. Только после фактического выполнения критериев публиковать аннотированный v1.0.2 и release без assets, обновить итоговый статус и оставить принятый runtime работающим.

## Документы и сохранённые материалы

[CURRENT](../../handoffs/CURRENT-STATUS.md), [эксплуатация](../../08-Runbook-эксплуатации.md), [операции C6](OPERATIONS.md), [очистка](CLEANUP-10.md), [реестр](../../handoffs/WORKSPACE-INVENTORY.md). Та же Word-памятка находится в каталоге NOBUS SPACE BOT; отдельная версия не создаётся.

Исторические статусы 06–09, исходные приёмочные критерии, неудачные попытки и доказательства сохраняются с их ревизиями. Уникальные 20 файлов основной копии и прежняя локальная main архивированы по разрешению на очистку. В Git не публикуются частные архивы, реальные запросы, аудио и данные пользователя. Глобальные настройки Codex, модели, credentials и интеграции не менялись.
