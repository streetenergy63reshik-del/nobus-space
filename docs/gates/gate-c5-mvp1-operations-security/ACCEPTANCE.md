# Gate C5 — приёмка

**DRAFT.** Полный L1 и независимые L2/L3 ещё не завершены; кандидат не опубликован.
Source SHA/tree, DOCX SHA-256, verification receipts и acceptance package связываются после freeze. [Область и факты](HANDOFF.md).

Критерии: A–F выполнены; нет открытых Critical/Major; Minor имеют влияние, workaround и владельца. RPO/RTO/retention приняты владельцем с ограничениями HANDOFF.
Новые source bytes не наследуют verdict прежней revision. FAIL сохраняются. C4 evidence применяются только в исходных identities и неизменных границах.
Обычный push/PR/merge после PASS разрешён. Force/direct main/admin bypass запрещены. Отсутствие CI не является CI PASS.
Publication receipt создаётся после merge отдельно, без самоссылочных SHA.
