# Независимая L2: CODE REJECT

Exact `e6190be47b1e880c87304774455e773c83470338`, tree `8264efbcf3e610b4d57c747b4e21a45f4d98e348`. Чистый detached checkout; код автора не изменялся.

**ISSUE-C3-L2-04, Major, IMPLEMENTATION_DEFECT:** `src/workers/codex_sdk.py:172` отменяет ожидание startup, а строки 545–560 считают немедленную очистку достаточной. Установленный SDK выполняет физический запуск через `asyncio.to_thread`; отмена await не останавливает поток. Пока Popen не вернулся, SDK close видит `_proc=None` и сообщает об успехе. Поток позднее публикует процесс, который уже не принадлежит adapter current/retired state.

Независимая проба использует настоящий код установленного SDK, заменяя Popen безопасной заглушкой. После timeout и успешного adapter.close поздний process остаётся живым: **1 FAIL**. Реальный процесс, модель, ASR и внешние действия не запускались. Требуется удерживать startup operation до фактического завершения, сохранять неизвестную очистку и повторно закрывать клиент после позднего завершения; отмена ожидания не доказывает остановку запуска.

Остальные проверки этой ревизии: **94 PASS** в startup/shutdown/SDK/retry/smoke/runtime_operations и **3 PASS** в собственных probes. Пропусков нет. `git diff --check` чистый.

Прежние L2-01/L2-02/R01/fixture findings закрыты. От fd5 изменены только startup/close методы двух source-файлов; прежние проверки recovery, C1/C2, результата, outbox и artifact переносятся лишь на неизменные области. Три фактических model result/replay остаются доказательствами соответствующих путей; они не доказывают исправность физического startup. Исторические отчёты и FAIL сохранены. Полные команды, counts, SHA-256, AST delta и границы переноса — в JSON рядом.
