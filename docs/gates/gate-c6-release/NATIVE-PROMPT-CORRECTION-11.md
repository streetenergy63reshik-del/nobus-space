# Согласованный разбор подготовки промпта

9 сентября 2026. **DRAFT: проверено локально и на реальной модели; ещё не опубликовано и не включено.**

По точному разрешению C6-ACT-09 опубликованная версия c89a986 включена после проверенных копий четырёх БД. Один Core/Job/poller, semantic, worker/ASR и локальная/публичная готовность прошли. Первая синтетическая задача подготовки промпта получила request_refused / HTTP409 до claim; 82 прежние задачи сохранены. После отказа контролируемый runtime штатно остановлен, admission hold включён; запрос не повторялся.

Изолированная диагностика воспроизвела TRUST_PROVENANCE_ROLE_CONFLICT: полный текст модель считала respond, а отдельно выделенную прямую инструкцию — transform_material. Проверка Core правильно не разрешила противоречивую операцию. Ошибка первой диагностической обёртки (строгая проверка Python-списков вместо JSON-массивов) исправлена отдельно; она не объявлена дефектом продукта.

В src/workers/codex_sdk.py уточнён только протокол смыслового разбора: подготовка промпта из предоставленного описания — transform_material как для полного ввода, так и для отдельно проверяемой вводной фразы. Операции и условия внутри материала остаются инертными. Правила сопоставления, проверка отсутствующего материала, полномочия, schema, SQL, модели и настройки не менялись.

Повторная изолированная обработка того же синтетического запроса на реальной модели дала два согласованных transform_material и EXECUTE/content.transform. Эта capability имеет effect_type=no_effect; поле effect_allowed=true не разрешает внешнюю запись. Проверка не создавала продуктовую задачу, не повторяла HTTP-запрос и не отправляла сообщение владельцу. Данные БД не изменились, контролируемое дерево модели завершено.

71 существующая проверка адаптера, приёма промптов и границы ответа прошла за 21,82 с; git diff --check PASS. Кандидат требует точечной независимой проверки именно этой правки и её связанного доказательства; старые source-проверки и активация не объявляются проверкой новых байтов.

До готовности остаются публикация и включение этого исправления, реальная задача/резервирование/перезапуск, три пользовательских сценария, наблюдение, явная итоговая приёмка, постоянная фаза и v1.0.2. C6-ACT-09 был точно подтверждён для c89a986; новая revision и совокупная оставшаяся квота оформляются после проверки кандидата. История отказа, исходные receipts и учтённый расход сохраняются.

Независимая проверка этого исходника завершена: [PASS и привязка](NATIVE-PROMPT-REVIEW-11.md). Текущее включение и приёмка отражены отдельно в [статусе](OPERATIONAL-STATUS-11.md).
