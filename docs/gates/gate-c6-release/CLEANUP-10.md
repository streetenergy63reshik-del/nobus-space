# Упорядочение проекта — 9 сентября 2026

Восемь устаревших рабочих копий удалены после сохранения и проверки архивов. Девять других копий перенесены целиком в закрытый архив: отдельные тестовые каталоги Windows не разрешил прочитать полностью, поэтому их содержимое не удалялось. Перед перемещением независимо проверены точные цели и границы; после — сохранение исходных каталогов, Git revision и незавершённых изменений.

Основная рабочая копия приведена к опубликованной `main`. Прежняя локальная `main` сохранена отдельной веткой. Все 20 незавершённых файлов сохранены с проверкой байтов, патчами, stash и Git bundle; в опубликованный продукт они не включены. История всех 21 исходных рабочих копий сохранена отдельными refs и проверенным bundle.

В работе оставлены основной репозиторий, C6, LIVE и C2: из C2 работающий профиль использует ASR-модель. Данные бота, резервные копии, результаты и загрузки пользователя, credentials, интеграции и смешанный каталог других клиентов исключены из удаления. Закрытый архив находится в локальном `.runtime/c6-entry/closure-20260909/private-cleanup-10`; в Git опубликовано только описание и хэши доказательств.

Проверка после очистки подтверждает целостность четырёх БД и прежние количества строк, включая 82 задачи. При этом процесс бота завершился в 14:29:40 UTC с `runtime_failed`. Точная причина не установлена; совпадение по времени не доказывает связь с очисткой. Последняя опубликованная версия ещё не включена. [Текущий статус](OPERATIONAL-STATUS-10.md) отражает эту остановку.

[Машиночитаемые итоги](CLEANUP-10.json). Удаление самих восстановимых архивов не входит в выполненную очистку.
