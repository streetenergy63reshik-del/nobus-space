# Проверка исправления смыслового разбора промпта

9 сентября 2026. **Точечная независимая проверка PASS**, блокеров нет. Проверен исходник `4d631dce25500e6bd3bfca0d31d769a858176e7f`, дерево `8bc574b40b052fbf42d27f1304d08cc7ada19003`, код `sha256:43a91fa406e0ca2131d0f4400feaa33eec9e88ae70c0d63f87a137568502a578`. Изменён только протокол адаптера в `src/workers/codex_sdk.py`.

Проверяющий сверил одинаковый синтетический запрос и хэши разборов реальной модели до/после. Прежняя пара respond/transform_material воспроизводит отказ, новая transform_material/transform_material допускает content.transform без внешнего эффекта. Независимые проверки в памяти подтвердили уточнение при пустом материале и прямом условии владельца, отказ на вложенное внешнее действие, поддельную ссылку и неверную границу. Неподдерживаемая операция остаётся недоступной. Проверен фактический метод адаптера и одинаковый протокол для двух разборов.

71 относящаяся к правке существующая проверка прошла за 21,82 с. Изолированная проверка на реальной модели дала согласованный результат; данные БД не изменились, дерево процесса завершено. Сам проверяющий не вызывал модель, ASR, SQL, LIVE или сеть. Старый полный набор не повторялся и не объявляется проверкой новых байтов.

Это проверка исходника, а не включения или итоговой приёмки. [Причина исправления](NATIVE-PROMPT-CORRECTION-11.md), [точная привязка](NATIVE-PROMPT-REVIEW-11.json), [фактическое состояние](OPERATIONAL-STATUS-11.md).
