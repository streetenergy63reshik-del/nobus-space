# Актуализация документации — 9 сентября 2026

Главная страница, индекс, документы продукта, глоссарий, архитектура, журнал ADR, эксплуатация, контекст продукта, действия владельца, текущий статус, реестр каталогов и передача C6 согласованы с публикацией PR №28, выполненной очисткой и фактической остановкой runtime. Длинные последовательности устаревших «текущих» состояний убраны из активных страниц; история и связанные доказательства остаются в Git и отдельных снимках C6.

Исходный код, схемы, статические ресурсы Mini App, sealed-нормативные документы, ADR и принятые пакеты C0–C5 не изменены. Код сохраняет digest `sha256:9f892f01d80b46f2570b3afb6fb66e5c8f185013183c2bde625068c5dc9bcfab`. Документ 11 входит в runtime inputs: новая активация должна привязать его новые байты к конфигурации backup и точной опубликованной revision. Прежняя привязка runtime не переиспользуется.

Проверяются существующие четыре проверки документации, разрешение ссылок и git diff --check. Обновление статуса не объявляет MVP1 готовым и не повторяет неизменный полный исходный цикл проверки. Указатель Nobus Memory не обновлялся: Git остаётся источником точного статуса.
