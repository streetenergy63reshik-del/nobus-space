# Git, GitHub и документация: упорядочивание 19 сентября 2026

Работа выполнена после принятия MVP1. База сверки — `c9390cc445d4e338d01552719dc6a02203c755a7`, опубликованные документы окончательной приёмки M1-S1. Runtime, задания Планировщика, модели, production-конфигурация и данные этой уборкой не изменялись.

## Что проверено и упорядочено

| Область | Результат |
|---|---|
| GitHub перед очисткой | Открытых PR нет; main и три релизных тега сверены |
| Удалённые ветки | Удалены 17 feature/docs веток, целиком вошедших в main; выполнен readback, теги неизменны |
| Локальные ветки | Удалены 14 слитых веток, не используемых worktrees; очистка только через `git branch -d`, без force |
| Локальная main | Устранено отставание от опубликованной истории через fast-forward после сохранения WIP; документация готовится в отдельной ветке и публикуется через PR |
| Отслеживаемые файлы | Проверено 789 файлов исходной ревизии, из них 439 в docs. Явных tracked caches, временных файлов, архивов или БД не обнаружено |
| Совпадающие файлы | Gate 0 fixtures/evidence, лицензии и C4 screenshots нужны своим manifest; сохранены с исходными путями и байтами |
| Актуальные документы | Согласованы README, продукт, архитектура, ADR index, runbook, инструкция владельца, Gate index, CURRENT и роли каталогов |
| Навигация | Разделены эксплуатация, контракты, будущие планы, исторические передачи и аудиты |
| Прежний WIP | Все 25 документов сохранены с хэшами в ZIP и stash; четыре старых локальных отчёта/квитанции вынесены из активных папок в частный архив |
| MVP2 | Уникальный ранее подготовленный план сохранён, актуализирован и публикуется только как PROPOSED / NOT STARTED; Gate не запускались |
| Nobus Memory | Неизменная [справка приёмки](MVP1-ACCEPTED-20260919.md) сохранена: на её точные байты уже ссылается запись памяти |
| Системный мусор | Добавлены исключения OS metadata в .gitignore. Пять файлов локального .pytest_cache, суммарно 3936 байт, НЕ УДАЛЕНЫ: Windows отказала в доступе при preflight. ACL не менялись; кэш не отслеживается Git |

Старые промежуточные OPEN и требования нового окна убраны из текущего краткого статуса. Полный прежний текст доступен [в Git на c9390cc](https://github.com/streetenergy63reshik-del/nobus-space/blob/c9390cc445d4e338d01552719dc6a02203c755a7/docs/handoffs/CURRENT-STATUS.md). Исторический журнал NOT PASS и исходные INCIDENT/EVIDENCE сохранены без изменения.

## Что сохранено намеренно

Удалённые ветки `codex/mvp1-c1-publication-status` (`a95cdfc`) и `codex/mvp1-closure-c1-semantic-compiler` (`8e5e5fd`) содержат по одному commit вне main. Их нельзя считать полностью слитыми только по названию закрытого Gate.

Все зарегистрированные worktrees, локальные archive/recovery/rollback refs и прежние stash сохранены. LIVE, четыре БД, backups, .venv, закреплённый production Python и C2 ASR-модель не являются мусором. Девять retained-originals содержат ранее недоступные тестовые каталоги; полная побайтовая архивная копия для них не доказана. [Роли и ограничения каталогов](WORKSPACE-INVENTORY.md).

Документ 11 входит в activation binding работающего продукта. Его историческая строка статуса сохранена вместе с принятыми байтами; актуальный статус читается в [CURRENT](CURRENT-STATUS.md). Изменение этого входа только ради редакционной уборки потребовало бы отдельного проверенного переключения production.

## Восстановимость

Частный каталог в каноническом checkout: `.runtime/repository-cleanup-20260919`. Он не публикуется и содержит точные refs/SHA до и после операций, план и квитанции, манифест 25 исходных файлов, binary patches, проверенные ZIP и Git bundle. Уникальные документы и история не уничтожены.

- `unpublished-documents.zip`, SHA-256 `0918497ed9fac689413bdfa7559a52af175da63b0102ba360ab2740fae761dd5`.
- `all-refs.bundle`, SHA-256 `d66e6385f56cad643abc55e1fdf8c06f1084e122e937b74387269615eaf4461d`; проверен `git bundle verify`, содержит исходные удалённые refs.
- `retired-local-documents/` — четыре старых локальных документа с подтверждённым совпадением исходных хэшей.

Восстановление отдельного документа выполняется в отдельную папку из ZIP, отдельной ветки — из bundle по сохранённому SHA; применять весь старый stash поверх актуальной main не следует.

## Проверки и границы

Область удаления независимо проверена: ancestry, exact refs, отсутствие использования локальных веток worktrees, наличие в bundle, хэши архивируемых документов, буквальные пути и отсутствие reparse points. Замечания к назначениям архива и сохранению источника Nobus Memory закрыты до действий.

Документные тесты: 4 PASS. Относительные Markdown-ссылки корректны; дополнительно проверены 19 локальных HTML-ссылок, 158 JSON и совпадение производной HTML с Markdown. Код, runtime inputs, исторические доказательства и источник записи Nobus Memory неизменны. Renderer и оформление HTML сохранены байт в байт из прежнего WIP; новая проверка браузерной матрицы не заявляется. `git diff --check` прошёл. Старые продуктовые L1–L3, модель, ASR, backup и эксплуатационная приёмка не повторяются. Эта уборка не является новым Gate или новым релизом MVP1.
